{
  "name": "bytefrost",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && pnpm install && pnpm run build",
    "start": "cd frontend && pnpm run start"
  },
  "devDependencies": {
    "shadcn": "^4.21.0"
  }
}
