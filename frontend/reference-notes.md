# Farm Minerals reference notes

The reference opens as a full-viewport canvas rather than a standard hero section. The first visible state uses a deep olive/green field, oversized editorial product typography, sparse top navigation, fine horizontal rules, and geometric cream blocks that mask/reveal the central visual. The central visual remains the focus while the page scroll drives changes inside the canvas.

The useful principles for KisanSetu are: full-screen visual dominance, very sparse navigation, a few oversized text moments, persistent fine rules, geometric masks/blocks, and transitions that feel like one scene changing state rather than cards entering a page. KisanSetu should retain its own field-green/cream palette, farmer imagery, direct-market narrative, and evidence language.

The updated KisanSetu top state now uses the reference-inspired dark olive canvas with oversized light serif typography, a full-height image field, thin divider rules, and geometric cream masks. A forward scroll held the same pinned composition while the image crossfaded toward harvest crates and the canvas progress advanced, confirming the intended visual principle is present without changing KisanSetu's story or assets.
