{
  "version": 2,
  "buildCommand": "cd ../kisansetu && pnpm install && pnpm run build && mkdir -p ../frontend/dist && cp -r dist/* ../frontend/dist/",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://bytefrost-backend.onrender.com/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
